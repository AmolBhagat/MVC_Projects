@model IEnumerable<Generate_Hyperlinks.Models.Employee>
@{
    ViewBag.Title = "Index";
}

<h2>Employee List</h2>
<ul>
    @foreach(var item in Model)
    {
        <li>@Html.ActionLink(item.Name,"Details",new { @id = item.ID})</li>
    }
</ul>
