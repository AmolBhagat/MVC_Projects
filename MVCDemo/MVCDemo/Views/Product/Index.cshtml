@using MVCDemo.Models;
@model IQueryable<Category>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<ul>
  @foreach (var item in Model)
  {
    <li>@Html.ActionLink(item.CategoryName, "productlist", new { cid = item.CategoryID })</li>
  }
</ul>